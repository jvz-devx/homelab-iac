---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # LibreChat is temporarily disabled to reset a failed first Helm install.
  # Re-enable after Flux prunes the failed release state.
  # - librechat
