---
apiVersion: v1
kind: Service
metadata:
  name: aiostreams
  namespace: aiostreams
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: aiostreams-hetzner
spec:
  selector:
    app.kubernetes.io/name: aiostreams
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
