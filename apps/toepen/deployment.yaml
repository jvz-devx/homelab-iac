apiVersion: apps/v1
kind: Deployment
metadata:
  name: toepen
  namespace: toepen
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: toepen
  template:
    metadata:
      labels:
        app.kubernetes.io/name: toepen
    spec:
      imagePullSecrets:
        - name: ghcr-auth
      terminationGracePeriodSeconds: 30
      containers:
        - name: toepen
          image: ghcr.io/jvz-devx/toepen:main@sha256:90d90db8176848dc35b541b6dc9f419014d0adce30fde046e87600517217723e # {"$imagepolicy": "flux-system:toepen"}
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: toepen-env
          env:
            - name: HOST
              value: 0.0.0.0
            - name: NODE_ENV
              value: production
            - name: ORIGIN
              value: https://toep.app
            - name: TRUST_PROXY
              value: "true"
            - name: PUBLIC_DISCORD_CLIENT_ID
              value: "630700637113286666"
            - name: PORT
              value: "3000"
            - name: VALKEY_URL
              value: redis://valkey:6379
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
