apiVersion: apps/v1
kind: Deployment
metadata:
  name: toepen
  namespace: toepen
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: toepen
  template:
    metadata:
      labels:
        app.kubernetes.io/name: toepen
    spec:
      imagePullSecrets:
        - name: ghcr-auth
      terminationGracePeriodSeconds: 30
      containers:
        - name: toepen
          image: ghcr.io/jvz-devx/toepen:main@sha256:e6650fb90942678946ce20160f3e07e487827c29c3c53230b635f29db97e0014 # {"$imagepolicy": "flux-system:toepen"}
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: toepen-env
          env:
            - name: HOST
              value: 0.0.0.0
            - name: NODE_ENV
              value: production
            - name: ORIGIN
              value: https://toep.app
            - name: PORT
              value: "3000"
            - name: VALKEY_URL
              value: redis://valkey:6379
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
