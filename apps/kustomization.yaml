---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Temporarily disabled after k3s recovery; app-level issues still need fixing.
  # - copyparty
  - aviary
  - cliproxyapi
  - hetzner-firewall-home-ip
  - liverpc-docs
  # Homelab OpenWebUI is disabled; do not keep a stale Hetzner Tailscale proxy alive.
  # - openwebui
  - resonance
  # - termix
